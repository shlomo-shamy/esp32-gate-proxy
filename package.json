{
  "name": "esp32-railway-proxy",
  "version": "1.0.0",
  "description": "Proxy server for ESP32 to Railway communication with TLS 1.3 support",
  "main": "proxy-server.js",
  "scripts": {
    "start": "node proxy-server.js",
    "dev": "nodemon proxy-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "esp32",
    "railway",
    "proxy",
    "iot",
    "tls"
  ],
  "author": "Gate Controller System",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}